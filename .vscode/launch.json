{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (CMake / MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${command:cmake.launchTargetDirectory}",
            "console": "integratedTerminal"
        }
    ]
}
